import { ArrowUpRight } from 'lucide-react'
import { projects } from '../data/profile'
import { SectionHeading } from './SectionHeading'

export function Projects() {
  return <section id="projects" className="section projects"><div className="container"><SectionHeading kicker="Selected work" title="Technical projects that support ERP outcomes." text="Examples of practical software and enterprise work. Project details can be expanded as more public case-study information becomes available."/><div className="projects-grid">{projects.map((project) => <article className="project-card" data-reveal key={project.title}><div><span className="project-label">{project.label}</span><h3>{project.title}</h3><p>{project.description}</p>{project.role && <p className="project-role"><strong>Role:</strong> {project.role}</p>}{project.stack && <div className="project-stack"><small>Tech stack</small><div className="tags">{project.stack.map((technology) => <span key={technology}>{technology}</span>)}</div></div>}{project.achievements && <details className="project-details"><summary>View key achievements</summary><ul>{project.achievements.map((achievement) => <li key={achievement}>{achievement}</li>)}</ul></details>}</div><div className="project-footer"><div className="tags">{project.tags.map((tag) => <span key={tag}>{tag}</span>)}</div><span aria-hidden="true" className="project-arrow"><ArrowUpRight size={18}/></span></div></article>)}</div></div></section>
}
